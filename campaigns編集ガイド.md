# campaigns.js の編集手順

キャンペーン情報を追加・修正・削除して、アプリに反映するまでの手順。
（AIの修正案や巡回PRの内容を「確認できたものだけ」手で反映するときに使う）

---

## 全体の流れ

1. `campaigns.js` をエディタで開いて書き換える
2. 保存する
3. ローカルで `index.html` を開いて、一覧が普通に表示されるか確認
4. GitHub Desktop で Commit → Push
5. Netlify にフォルダを再アップロード

---

## 1. ファイルを開く

デスクトップの「見逃さん」フォルダの **`campaigns.js`** を右クリック →「プログラムから開く」→ **VS Code**。
（メモ帳でも可。VS Code なら打ち間違いを赤線で教えてくれるので推奨）

---

## 2. 書き換える

キャンペーン1件は、`{` から `},` までの「かたまり」です。

```js
  {
    id: 3006, service: "Spotify", icon: "🎶", category: "音楽",
    title: "Premium Standard 3か月無料", detail: "これまでにPremiumを利用したことがない人が対象。終了後は月額1,080円",
    url: "https://www.spotify.com/jp/premium/",
    deadline: "2026-09-23", freeDays: 90, hasCancellation: true, addedAt: "2026-08-29",
    tags: ["tanshin"],
    lastChecked: "2026-08-29", source: "公式",
  },
```

### A. 値を直す（例：締切日が変わった、無料日数が変わった）

1. `Ctrl+F` でサービス名（例「Spotify」）を検索
2. 直したい項目を書き換える。例：`deadline: "2026-09-23"` → `deadline: "2026-10-31"`
3. **確認した日を `lastChecked` に入れ直す**。例：`lastChecked: "2026-09-15"`
4. `Ctrl+S` で保存

### B. 新しく1件足す

1. 既存の1かたまり（`{` から `},`）をコピー
2. ファイル末尾の `];` の **すぐ上** に貼り付け
3. 値を全部書き換える
4. `id:` は **他と重複しない数字**にする（例：`3200`, `3201` …）
5. 保存

各項目の意味 → `campaigns.js` 冒頭のコメント参照。最低限：

| 項目 | 入れるもの |
|---|---|
| `id` | 他と重複しない数字。既存のものは変えない |
| `service` / `title` / `detail` | サービス名・見出し・補足（公式の文章は転載せず自分の言葉で） |
| `category` | `動画` `音楽` `雑誌` `フード` `買い物` `くらし` `ゲーム` のいずれか |
| `url` | 公式の案内ページ（実在URLのみ） |
| `deadline` | 受付締切日 `"2026-10-31"`。締切の告知がない通年トライアルは `null`（クォートなし） |
| `freeDays` | 無料期間の日数。無料期間がない還元系は `null` |
| `hasCancellation` | 解約の概念があれば `true`、還元系など不要なら `false` |
| `addedAt` | 載せた日 `"2026-09-15"` |
| `lastChecked` / `source` | 確認した日 と `"公式"` / `"運営確認"` / `"まとめ記事・要公式確認"` |

### C. 1件消す

その `{` から `},`（**末尾のカンマ含む**）までを選択して削除 → 保存。

---

## 3. 壊さないための注意

- 各かたまりの終わりの **`,`（カンマ）を消さない**（最後の1件も `},` でよい）
- 文字は `"..."` で囲む。日付も `"2026-09-23"`。`null` はクォートなし
- 先頭の `window.CAMPAIGNS = [` と、末尾の `];` は消さない
- 全角の引用符（`”` `“`）はNG。半角の `"` を使う（VS Code なら赤線が出る）
- 保存後、ローカルで `index.html` を開いて一覧が出るか確認。
  **空・崩れていたら打ち間違い**（多くはカンマ抜け・クォート抜け）。VS Code の赤線を見て直す

---

## 4. 反映する

1. **GitHub Desktop**
   - 変更ファイルに `campaigns.js` が出る
   - 左下 Summary に「キャンペーン更新」など短く入力
   - **Commit to main** → **Push origin**
2. **Netlify**
   - GitHub Desktop が「Fetch/Pull」を促したら Fetch → Pull を先に
   - Netlify の Deploys 画面に「見逃さん」フォルダをドラッグ＆ドロップ

---

## AIの修正案・巡回PRを反映するとき

1. PR本文（`LAST_REPORT.md`）の「🔺 変化あり」「🤖 修正案」を読む
2. **その値を、必ず公式サイトで自分で確認**（AIや差分は下書き。正確性は運営の責任）
3. 確認できたものだけ、上の A/B/C の手順で `campaigns.js` に反映
4. 確認できないもの・怪しいものは反映しない（削除は特に慎重に）
5. PR は Merge（スナップショットが更新され、次回はそこからの差分になる）
